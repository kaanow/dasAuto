# Decisions Log — theo

> Append-only narrative of the journey. New sessions add entries at the
> bottom. Older entries don't get edited (they're the historical record).

## 2026-06-09 — Session 1 (intake + full buildout)

Brief delivered (now at `family.md`). Family of 4 (2 adults + 2 young
teens), Kamloops BC, ICBC Territory L / class 003 / CDF 0.686, home EV
charging available. ~15,300 km/yr, city-dominant, with ~12 corridor trips/yr
(400–500 km) plus an occasional 800 km trip 1–2×/yr. Current car: 2018 Toyota
Corolla. **TCO over 10 years is the primary metric; capital cost subordinate.**
Powertrains all open (EV / ICE / (P)HEV); PHEV drivetrain type to be noted.
Cargo is the headline functional requirement (frunk a bonus). AWD or FWD both
acceptable.

Built the full deployment in one session:

- `site.json` — "Theo finds a different car", Kamloops BC, Territory L / CDF
  0.686 insurance ref.
- `weights.json` — cargo raised to 2.0, family-fit/third-row/fsr/hitch
  minimised, TCO 3.0 (see `cohort_rationale.md` for the per-weight rationale).
- `research/tco_research.md` — Kamloops gas adjustment (1.62 vs Vancouver's
  1.79, no TransLink levy), BC Hydro electricity unchanged, and the
  insurance model anchored to Theo's real $1,764/yr Corolla quote
  (`ins_annual = 1450 + 0.020 × pretax`, cross-checked against kaan's
  numbers × the 1.30 driver-factor ratio).
- `vehicles.json` — 19 entries (8 BEV / 4 PHEV / 4 HEV / 2 ICE + the current
  Corolla baseline), generated by `build_cohort.py` so the fuel/insurance/
  residual/on-road anchors are formula-consistent.

### Decisions made (no family conflict surfaced)

- **AWD is a scored criterion, not a hard filter** — the brief explicitly
  allows FWD. Applied the selection principle from day one; no AWD-bug
  repeat.
- **Kamloops gas discount** (~10% under Vancouver) applied to gas-burning
  entries; electricity/DCFC/discount unchanged (province-level). No `tco.py`
  change needed — the escalation shape is BC-wide; only the stored anchors
  differ.
- **Insurance** anchored to the family's actual $1,764/yr full-coverage
  quote rather than reconstructing ICBC tables from scratch.
- **Kept honest losers in the cohort** (Bolt EUV, Ioniq 6) to make the
  cargo-vs-efficiency and cheap-vs-capable trades visible rather than
  pre-filtering them out.

### Ranking at launch (10-yr horizon, Theo's weights)

Top of board: Model Y (used), RAV4 Hybrid (new), CR-V Hybrid (new), Prius
(new), RAV4 Prime (new). Lowest TCO overall is the Bolt EUV (but it ranks
~15th on cargo/corridor/winter). Gas-only CX-5/Outback rank low under the
TCO-primary weighting. The 2018 Corolla baseline lands mid-pack — most
upgrades justify their premium.

### Deferred / remaining

- **Images** — `image_seeds.json` not yet built; the app runs with
  placeholder galleries until Wikimedia URLs are curated per vehicle.
- **Railway service** — needs provisioning in the dashboard
  (`VEHICLE_DATA_DIR=user-theo`, `VEHICLE_DB_PATH=/data/cache.db`, 1 GB
  volume) + CNAME `brudersauto.alti2.de`. Dashboard action for the user.
- **Cross-shop** — deferred until Theo converges on a nameplate (per
  `docs/multi_family_todo.md`).
