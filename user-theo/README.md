# user-theo

Family folder for the brother's deployment at `brudersauto.alti2.de`.

**Current state:** cohort built (Session 1, 2026-06-09). 19-vehicle cohort
ranked and rendering locally. Remaining: image curation + Railway service
provisioning.

## What's here

- `family.md` — canonical brief (don't edit; clarifications → `research/decisions_log.md`)
- `site.json` — display identity (Kamloops BC, Territory L)
- `weights.json` — cargo-and-TCO-weighted criteria
- `vehicles.json` — the 19-vehicle cohort (generated by `build_cohort.py`)
- `build_cohort.py` — regenerates `vehicles.json` from the research table + formulas
- `research/` — `tco_research.md`, `cohort_rationale.md`, `decisions_log.md`

## Remaining to go fully live

1. **Images** — build `image_seeds.json` (5–7 Wikimedia URLs per vehicle),
   run `scrapers/fetch_images.py`. App runs with placeholder galleries until then.
2. **Railway service** — new service off this repo/branch with
   `VEHICLE_DATA_DIR=user-theo`, `VEHICLE_DB_PATH=/data/cache.db`,
   `PYTHONUNBUFFERED=1`, a 1 GB volume at `/data`. See root `README.md` §
   "Deploying to Railway".
3. **CNAME** `brudersauto.alti2.de` → the Railway host.

## Run locally

```bash
VEHICLE_DATA_DIR=$(pwd)/user-theo bash vehicle-app/run.sh
```
